import React from "react";

function Memo() {
  return (
    <div>
      <h1>建立 JSON Server 用 useEffect 模擬抓取、送出與刪除資料</h1>
      <h3>實際撰寫</h3>
      <p>npx json-server --watch data/db.json --port 8000</p>
    </div>
  );
}

export default Memo;
